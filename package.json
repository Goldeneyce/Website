{
  "name": "everest-world-lifts-website",
  "version": "2.0.0",
  "private": true,
  "description": "Everest World Lifts marketing website",
  "scripts": {
    "build": "npm run images && eleventy",
    "dev": "npm run images && eleventy --serve",
    "images": "node scripts/optimize-images.mjs",
    "check": "npm run build && node scripts/validate-site.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "sharp": "^0.34.3"
  }
}
